# .gitignore is restricted to the artifacts produced by go build and test.
# for personal setup you can use personal .gitignore: `git config --global core.excludesfile ~/.gitignore`

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Example binaries (Linux extensionless, produced by go build ./examples/...)
petinsurance

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Go workspace file
go.work
go.work.sum
